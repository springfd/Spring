<div class="container">
	<div class="row">
		<div class="col-md-12">
			<h3 style="letter-spacing: 2px;"><%= Hash[projectKindOptions].rassoc(params[:pj_kind].to_i).first%></h3>
			<%= link_to "新增",{controller: 'projects', action: 'new', pj_kind: params[:pj_kind]}, data: {no_turbolink: true}, class: 'add-btn pull-right' %>		
		</div>
	</div>
</div>
<div class="container">
	<div class="row">
	    <% @projects.each do |project| %>
	    	<%= render "sub_project_table", project: project %>
	    <% end %>
    </div>
</div>
  
