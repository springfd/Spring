<%= form_for(@link) do |f| %>
<div class="container" style="margin-top: 10mm">
  <% if @link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group row">
    <label class="col-sm-offset-1 col-sm-2 control-label">連結</label>
    <div class="col-sm-4"><%= f.text_field :name, class: "form-control", placeholder: '連結' %></div>
  </div>
  <div class="form-group row">
    <label class="col-sm-offset-1 col-sm-2 control-label">網址</label>
    <div class="col-sm-4"><%= f.text_field :web_addr, class: "form-control", placeholder: '網址' %></div>
  </div>
  <div class="form-group row">
		<div class="col-sm-offset-8 col-sm-10">
			<%= f.submit '儲存',  class: "btn btn-default"  %>
		</div>
  </div>
</div>
<% end %>
