<script>
	var controller = new ScrollMagic.Controller();
//============================= slogan =============================//

	tween = new TimelineLite();
	tween.fromTo(".left_vertical_solgan_line", 1, {height:0, ease: Linear.easeNone}, {height: '100%', ease: Linear.easeNone})
		 .fromTo(".top_horizontal_solgan_line", 1, {width:0, ease: Linear.easeNone}, {width:'100%', ease: Linear.easeNone})	  	 
		 .fromTo(".right_vertical_solgan_line", 1, {height:0, ease: Linear.easeNone}, {height:'100%', ease: Linear.easeNone})
		 .fromTo(".bottom_horizontal_solgan_line", 1, {width:0, ease: Linear.easeNone}, {width:'100%', ease: Linear.easeNone})	  	 		 	 	
		 .to(".slogan_1", 1, {autoAlpha: 1, ease: Linear.easeNone})
		  .to(".slogan_2", 1, {autoAlpha: 1, ease: Linear.easeNone})
		   .to(".slogan_3", 1, {autoAlpha: 1, ease: Linear.easeNone})
		    .to(".slogan_4", 1, {autoAlpha: 1, ease: Linear.easeNone})
		 .to(".about_arrow_container", 1, {autoAlpha: 1, ease: Linear.easeNone})
	tween.timeScale(3) //play faster   			
	var scene = new ScrollMagic.Scene({triggerElement: ".about_top", tweenChanges: true})
					.setTween(tween)
					.addTo(controller);	

//============================= click =============================//
$('a[href^="#"]').on('click',function (e) {
	e.preventDefault();
	var target = this.hash;
	var $target = $(target);	
	$('html, body').stop().animate({
		'scrollTop': $target.offset().top
		}, 900, 'swing', function () {
			//window.location.hash = target;
	});
});
//============================= arrow =============================//
var tween = new TimelineMax()
				.fromTo(".about_arrow span", 1,
						{top: '0px'},
						{top: '10px', repeat: -1, yoyo: true, ease: Circ.easeInOut})				
				//.to(".donation_content_donate_inner", 1, {autoAlpha: 1, ease: Linear.easeNone})

//============================= more =============================//
	$('#chair_more_content').hide()
	$('#chair_more').click(function() {
  		$('#chair_more_content_less').hide()		
  		$('#chair_more_content').show('slow')
	});
	$('.ceo_more_content').hide()
	$('.ceo_more').click(function() {
  		$('.ceo_more_content_less').hide()		
  		$('.ceo_more_content').show('slow')
	});	

	// init controller


	// build tween
	/*
	var tween = new TimelineMax()
					.to(".about_quote_1", 1, {autoAlpha: 1, ease: Linear.easeNone})
	tween.timeScale(2) //play faster 
	var scene = new ScrollMagic.Scene({triggerElement: "#about_quote_title_1", tweenChanges: true})
					.setTween(tween)
					.addTo(controller);	
	var tween = new TimelineMax()
					.to(".about_quote_2", 1, {autoAlpha: 1, ease: Linear.easeNone})
	tween.timeScale(2) //play faster 
	var scene = new ScrollMagic.Scene({triggerElement: ".about_quote_title_2", duration: 200, tweenChanges: true})
					.setTween(tween)
					.addTo(controller);	
					
	var tween = new TimelineMax()
					.to(".about_quote_2_mobile", 1, {autoAlpha: 1, ease: Linear.easeNone})
	tween.timeScale(2) //play faster 
	var scene = new ScrollMagic.Scene({triggerElement: ".about_quote_title_2_mobile ", duration: 200, tweenChanges: true})
					.setTween(tween)
					.addTo(controller);															
										
	var tween = new TimelineMax()
					.to(".about_original_1_container", 1, {autoAlpha: 1, ease: Linear.easeNone})
	var scene = new ScrollMagic.Scene({triggerElement: ".about_original_1_container", duration: 200, tweenChanges: true})
					.setTween(tween)
					.addTo(controller);							
	var tween = new TimelineMax()
					.to(".about_original_2_container", 1, {autoAlpha: 1, ease: Linear.easeNone})	
	tween.timeScale(2) //play faster	
	var scene = new ScrollMagic.Scene({triggerElement: ".about_original_2_container", tweenChanges: true})
					.setTween(tween)
					.addTo(controller);			
	var tween = new TimelineMax()
					.to(".about_original_2_container_mobile", 1, {autoAlpha: 1, ease: Linear.easeNone})	
	tween.timeScale(2) //play faster					
	var scene = new ScrollMagic.Scene({triggerElement: ".about_original_2_container_mobile", tweenChanges: true})
					.setTween(tween)
					.addTo(controller);									
	*/

//============================= project =============================//

<% for i in 1..8 %>
	tween = new TimelineLite();

	tween.to(".plan_card_container_<%=i%>", 1, {autoAlpha: 1, ease: Linear.easeNone})	
		 //.from(".plan_card_container_<%=i%>", 1, {right: "20px", ease: Linear.easeNone})	
	tween.timeScale(2) //play faster
	new ScrollMagic.Scene({triggerElement: ".plan_card_container_<%=i%>"})
				  .setTween(tween)
				  .addTo(controller) 		  
<% end %>

<% for i in 1..2 %>
	tween = new TimelineLite();
	//.to(".about_plan_main_container_<%=i%>", 1, {autoAlpha: 1, ease: Linear.easeNone})	
	tween.from(".about_plan_main_container_<%=i%>", 1, {top: "30px", ease: Linear.easeNone})	
	tween.timeScale(2) //play faster		 	 
	new ScrollMagic.Scene({triggerElement: ".about_plan_main_container_<%=i%>"})
				  .setTween(tween)
				  .addTo(controller) 		  
<% end %>
					
//============================= masonry =============================//
	var $grid = $('.grid-plan').masonry({
	  itemSelector: '.grid-plan-item',
	  columnWidth: '.grid-plan-sizer',
	  percentPosition: true
	});
	
	/*
	$grid.on( 'click', '.grid-plan-item-button', function( event ) {
	  $( event.currentTarget ).closest('.grid-plan-item').toggleClass('is-expanded');
	  $grid.masonry();
	});
	
	$grid.on( 'click', '.grid-plan-item-close', function( event ) {
	  $( event.currentTarget ).closest('.grid-plan-item').toggleClass('is-expanded');
	  $grid.masonry();
	});
	*/				
</script>