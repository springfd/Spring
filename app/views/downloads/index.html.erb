<div class="container">
	<div class="row">
		<div class="col-md-12">	
			<h3 style="letter-spacing: 2px;">文件下載</h3>
			<%= link_to "新增", new_download_path, class: 'add-btn pull-right' %>
		</div>		
	</div>
</div>
<div class="container">
	<div class="row">
	<% @downloads.each do |download| %>
		<%= render "sub_download_table", download: download %>
	<% end %>
	</div>
</div>
