<div class="container">
	<div class="row">
		<h3>捐款</h3>
	</div>
</div>
<div class="container-fluid">
	<div class="row">	
		<div class="col-md-offset-9">
			<%= link_to "新增", new_donation_path, class: 'btn btn-info add-btn' %>
		</div>
	</div>
</div>
<div class="container">
	<% @donations.each do |donation| %>
		<%= render "sub_donation_panel", donation: donation %>
	<% end %>
</div>

